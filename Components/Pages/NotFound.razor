@page "/not-found"
@layout MainLayout

<PageHeader Title="Not found" />

<Card>
    <h3 style="font-size:var(--text-lg);margin-bottom:var(--space-2);">Not found</h3>
    <p style="color:var(--text-secondary);font-size:var(--text-sm);">
        The content you are looking for does not exist.
    </p>
</Card>
