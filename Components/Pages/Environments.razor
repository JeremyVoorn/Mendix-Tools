@* MT-07 shell placeholder — the app home ("/"). Replaced by the real dashboard in
   MT-10 (mock) / MT-20 (wired). Proves nav routing, the active state, and the topbar
   title + subtitle plumbing. *@
@page "/"

<PageHeader Title="Environments" Subtitle="placeholder · wired in MT-10" />

<Card Title="Environments" Subtitle="Coming soon">
    <p style="color:var(--text-secondary);font-size:var(--text-sm);">
        The environments dashboard lands in MT-10 (mock data) and MT-20 (wired to Deploy
        API v1). This placeholder confirms the shell routes here as the app home.
    </p>
</Card>
